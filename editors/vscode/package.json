{
  "name": "pyfun",
  "displayName": "Pyfun",
  "description": "Pyfun language support: syntax highlighting plus diagnostics, hover, navigation, and more, backed by `pyfun lsp`.",
  "version": "0.0.12",
  "publisher": "pyfun",
  "repository": {
    "type": "git",
    "url": "https://github.com/simontreanor/Pyfun.git",
    "directory": "editors/vscode"
  },
  "homepage": "https://github.com/simontreanor/Pyfun#readme",
  "bugs": {
    "url": "https://github.com/simontreanor/Pyfun/issues"
  },
  "license": "Apache-2.0",
  "icon": "icon.png",
  "galleryBanner": {
    "color": "#272822",
    "theme": "dark"
  },
  "keywords": [
    "functional",
    "fsharp",
    "ml",
    "language",
    "lsp",
    "pyfun",
    "transpiler"
  ],
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": [
    "Programming Languages"
  ],
  "main": "./extension.js",
  "activationEvents": [
    "onLanguage:pyfun"
  ],
  "contributes": {
    "languages": [
      {
        "id": "pyfun",
        "aliases": [
          "Pyfun",
          "pyfun"
        ],
        "extensions": [
          ".pyfun"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "pyfun",
        "scopeName": "source.pyfun",
        "path": "./pyfun.tmLanguage.json"
      }
    ],
    "configuration": {
      "title": "Pyfun",
      "properties": {
        "pyfun.server.path": {
          "type": "string",
          "default": "pyfun",
          "description": "Path to the `pyfun` executable used to launch the language server (`pyfun lsp`). Defaults to `pyfun` on PATH; during development point it at target/debug/pyfun."
        }
      }
    },
    "configurationDefaults": {
      "editor.tokenColorCustomizations": {
        "textMateRules": [
          {
            "comment": "Declaration keywords `let`/`fun`/`type`/`measure`/`module` — the 'introduces a name' family, pinned to cyan (Monokai's `let` colour) so they read uniformly across themes. Control-flow keywords and the `mut`/`<-`/`extern` escape hatches stay pink.",
            "scope": [
              "keyword.declaration.pyfun"
            ],
            "settings": {
              "foreground": "#66D9EF",
              "fontStyle": ""
            }
          },
          {
            "comment": "`mut`/`<-`/`extern` — the mutability + FFI escape hatches, pinned to Monokai's keyword pink, matching Rust's `mut` (a `storage.modifier`).",
            "scope": [
              "storage.modifier.mutable.pyfun",
              "keyword.operator.mutation.pyfun",
              "keyword.other.extern.pyfun"
            ],
            "settings": {
              "foreground": "#F92672",
              "fontStyle": ""
            }
          },
          {
            "comment": "Computation-expression builders `async`/`seq`/`result` — pinned to Monokai purple to give the CE openers their own identity, distinct from keywords and declarations.",
            "scope": [
              "keyword.other.computation.pyfun"
            ],
            "settings": {
              "foreground": "#AE81FF",
              "fontStyle": ""
            }
          },
          {
            "comment": "Unit-of-measure annotations `<m>`, `<m/s^2>` — pinned to Monokai orange so dimensional units stand out from types and keywords.",
            "scope": [
              "storage.type.unit.pyfun"
            ],
            "settings": {
              "foreground": "#FD971F",
              "fontStyle": ""
            }
          },
          {
            "comment": "Control-flow keywords (`if`/`then`/`else`/`elif`/`match`/`case`/`with`/`return`/`yield`/`do`/`in`) and the logical `and`/`or`/`not` — pinned to bracket-gold so control flow stays distinct and pink remains reserved for the `mut`/`<-`/`extern` escape hatches.",
            "scope": [
              "keyword.control.pyfun",
              "keyword.operator.logical.pyfun"
            ],
            "settings": {
              "foreground": "#FFD700",
              "fontStyle": ""
            }
          }
        ],
        "[*Light*]": {
          "comment": "Light-theme overrides: the Monokai palette above is tuned for a dark background and washes out on white. These keep each role's hue family but darken to WCAG-AA contrast (>= ~4.9:1 on white). This block only applies to themes whose name contains `Light`; dark themes keep the values above.",
          "textMateRules": [
            {
              "comment": "Declarations — teal, the darkened cyan.",
              "scope": [
                "keyword.declaration.pyfun"
              ],
              "settings": {
                "foreground": "#0E7490",
                "fontStyle": ""
              }
            },
            {
              "comment": "Escape hatches (`mut`/`<-`/`extern`) — deep magenta, the darkened pink.",
              "scope": [
                "storage.modifier.mutable.pyfun",
                "keyword.operator.mutation.pyfun",
                "keyword.other.extern.pyfun"
              ],
              "settings": {
                "foreground": "#C71D6C",
                "fontStyle": ""
              }
            },
            {
              "comment": "CE builders (`async`/`seq`/`result`) — violet, the darkened purple.",
              "scope": [
                "keyword.other.computation.pyfun"
              ],
              "settings": {
                "foreground": "#7C3AED",
                "fontStyle": ""
              }
            },
            {
              "comment": "Units of measure — burnt orange, the darkened orange.",
              "scope": [
                "storage.type.unit.pyfun"
              ],
              "settings": {
                "foreground": "#C2410C",
                "fontStyle": ""
              }
            },
            {
              "comment": "Control flow + logical — dark amber, the darkened gold (bright gold is near-invisible on white).",
              "scope": [
                "keyword.control.pyfun",
                "keyword.operator.logical.pyfun"
              ],
              "settings": {
                "foreground": "#9A6700",
                "fontStyle": ""
              }
            }
          ]
        }
      }
    }
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  }
}
